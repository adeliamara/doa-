<!DOCTYPE html>
<html>
<head>
  <title><%= t('home.title') %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>

<body>
  <header>
    <nav>
      <%= link_to t('home.title'), root_path %> |
      <%= link_to t('donations'), donations_path %> |
      <%= link_to t('projects'), projects_path %> |
      <%= link_to t('donors'), donors_path %> |
      <%= link_to t('expenses'), expenses_path %>
    </nav>
  </header>

  <main>
    <h1><%= t('home.welcome') %></h1>
    <p>Estamos felizes em tê-lo aqui. Explore nossos projetos, faça doações e ajude-nos a fazer a diferença!</p>
  </main>

  <footer>
    <p>&copy; <%= Time.now.year %> <%=  t('project_title') %>. <%= t('home.footer') %></p>
  </footer>
</body>
</html>
